# Placeholder Modal secrets configuration. Replace values before deploying.
secrets:
  MODAL_API_KEY: ${MODAL_API_KEY}
  SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}
  STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}

